# Set prefix to space
unbind C-b
set -g prefix C-Space

# Setting to 256 Colour
set -g default-terminal "screen-256color"

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
bind-key -n C-h if-shell "$is_vim" "send-keys C-h"  "select-pane -L"
bind-key -n C-j if-shell "$is_vim" "send-keys C-j"  "select-pane -D"
bind-key -n C-k if-shell "$is_vim" "send-keys C-k"  "select-pane -U"
bind-key -n C-l if-shell "$is_vim" "send-keys C-l"  "select-pane -R"
bind-key -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"
bind-key -T copy-mode-vi C-h select-pane -L
bind-key -T copy-mode-vi C-j select-pane -D
bind-key -T copy-mode-vi C-k select-pane -U
bind-key -T copy-mode-vi C-l select-pane -R
bind-key -T copy-mode-vi C-\ select-pane -l



# Setting toggle to ctrl-sp sp
bind h prev
bind l next
bind C-h prev
bind C-l next
bind C-Space next

# Set new split buttons
bind \ split-window -h -c '#{pane_current_path)'
bind | split-window -h -c '#{pane_current_path)'
bind - split-window -v -c '#{pane_current_path)' 

# Changing new window command to n
unbind n
bind n new-window

# Toggling command to synchronize panes which will send inputs to all visible panes
unbind c
bind-key c set-window-option synchronize-panes

# Enable vim mode in window
# setup hotkeys
set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
# bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

# Status bar Styling
set -g status-fg '#999999'
set -g status-bg default
set -g window-status-current-fg '#009099'
set -g status-right ''

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
